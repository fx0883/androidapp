<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.childhealthdiet.app2">

    <application
        android:name=".RecipeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activitys.RecipeKeywordListActivity" />
        <activity android:name=".ui.activitys.BasketActivity" />
        <activity
            android:name=".ui.activitys.RecipeDetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.activitys.RecipeSearchActivity"
            android:label="@string/title_activity_recipe_search"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.activitys.KidEidtActivity"></activity>
    </application>

</manifest>